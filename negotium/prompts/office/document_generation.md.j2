{{ context }}

{% if readable_context %}
선택된 내부 정보 (AI가 우선 읽을 원천):
{{ readable_context }}
{% endif %}
{% if attachment_context %}
첨부 파일에서 추출한 내용 (이미지는 본문 메시지에 함께 전달됨):
{{ attachment_context }}
{% endif %}

문서 유형: {{ document_label }}
제목: {{ title }}
대상 독자: {{ audience or "(미지정)" }}
원문/메모:
{{ source_text }}

작성 지침:
- 회사 업무에 바로 사용할 수 있는 문서로 작성하세요.
- 핵심 요약, 본문, 액션 아이템, 확인 필요사항을 포함하세요.
- 첨부 파일(이미지/PDF/표 등)이 있으면 그 내용을 반영하세요.
{% if output_format == "auto" %}
- 출력 형식은 내용에 가장 적합한 형식을 직접 선택하세요. 표 형태 데이터면 csv, 구조화 데이터면 json, 웹 표시는 html, 그 외는 markdown을 사용합니다.
- 출력의 첫 줄에 반드시 선택한 형식을 다음과 같이 명시하세요: `<!-- negotium:format=markdown -->` (markdown/html/csv/json/text 중 하나).
- 그 다음 줄부터 실제 문서 본문만 작성하세요.
{% else %}
- 출력 형식은 반드시 {{ output_format }} 로 작성하세요.
- 출력의 첫 줄에 `<!-- negotium:format={{ output_format }} -->` 를 명시한 뒤 본문을 작성하세요.
{% endif %}
